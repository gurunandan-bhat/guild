{{- $guild := slice }}
{{- range .Sections }}
    {{- range .Pages }}
        {{- $pageMap := (jsonify . ) | transform.Unmarshal }}
        {{- $pageMap = merge $pageMap (dict "ReviewURL" (urlize .LinkTitle)) }}
        {{- $guild = append $pageMap $guild }}
    {{- end }}
{{- end }}
{{- $guild | jsonify (dict "prefix" "" "indent" "\t") }}
